export interface ITodo {
    title: string,
    id: number,
    complited: boolean,
}

export interface IPhoto{
    url:string,
    id:number,
}

export interface ISong{
    url:string,
    name:string,
}